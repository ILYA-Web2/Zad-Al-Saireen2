-- ═══════════════════════════════════════════════════════════════════════
-- إصلاح أمني عاجل — يُشغَّل في Supabase SQL editor بعد مراجعته
-- (مرتبط بالدفعة 1 من تقرير الفحص: "كل سياسات RLS مفتوحة للجميع")
-- ═══════════════════════════════════════════════════════════════════════
--
-- المشكلة: كل الجداول كانت تسمح بالكتابة/التعديل/الحذف العام
-- (`using (true)`) لأي شخص يملك مفتاح anon/publishable — وهو مضمّن في
-- الكود المصدري ويمكن استخراجه من أي نسخة منشورة من التطبيق.
--
-- هذا الملف يزيل صلاحيات الكتابة العامة عن الجداول الأكثر حساسية
-- (remote_config والإشعارات ومفاتيح YouTube)، لأن هذه الثلاثة قادرة على
-- التأثير على *كل* مستخدمي التطبيق دفعة واحدة (رابط تحديث مزيّف، تعطيل
-- التطبيق بالكامل، حقن مفاتيح API خبيثة). القراءة العامة تبقى مسموحة —
-- هذه البيانات ليست حساسة عند القراءة فقط.
--
-- ⚠️ بعد تشغيل هذا الملف: لوحة التحكم داخل التطبيق (الكتابة المباشرة عبر
-- anon key) لن تعمل بعد الآن لهذه الجداول الثلاثة. يجب نقل كل عملية كتابة
-- إليها لتمر عبر Supabase Edge Function موقّعة بمفتاح service_role (الذي
-- لا يُخزَّن أبداً داخل التطبيق). راجع ملف admin-actions-edge-function.md
-- المرفق للتفاصيل والكود الجاهز.

-- ─── app_remote_config ─────────────────────────────────────────────────
-- (الأخطر: هذا الجدول يتحكم بـ force_update / update_url الذي يظهر
-- كشاشة إجبارية غير قابلة للتجاوز لكل مستخدمي التطبيق)
drop policy if exists "Public write access" on app_remote_config;

-- تبقى القراءة عامة (كل تطبيق يحتاج قراءتها عند الإقلاع)
-- create policy "Public read access" on app_remote_config for select using (true); -- موجودة أصلاً

-- ─── admin_notifications ───────────────────────────────────────────────
drop policy if exists "Public write access" on admin_notifications;
drop policy if exists "Public update access" on admin_notifications;

-- ─── youtube_api_keys ──────────────────────────────────────────────────
drop policy if exists "Public write access" on youtube_api_keys;
drop policy if exists "Public update access" on youtube_api_keys;
drop policy if exists "Public delete access" on youtube_api_keys;

-- ملاحظة: القراءة العامة (`Public read access`) تبقى كما هي على الثلاثة
-- جداول أعلاه — هذا مقصود ومطلوب لعمل التطبيق العادي.

-- ═══════════════════════════════════════════════════════════════════════
-- بيانات المستخدم (favorites / downloads / search_history / user_settings
-- / app_users) — لم تُغيَّر في هذا الملف عمداً، لأن تقييدها الصحيح يتطلب
-- أولاً إضافة عمود device_id لكل جدول لا يملكه فعلياً (favorites/downloads
-- لا يملكانه حالياً)، ثم بناء دالة تحقق حقيقية (Edge Function) بدل الاعتماد
-- على RLS وحدها بلا نظام هوية حقيقي خلفها. هذا جزء من "الدفعة 5: إعادة
-- البناء الأمني" في خطة العمل المتفق عليها — تغييره الآن دون تلك الخطوة
-- المرافقة سيكسر الميزات الحالية (المفضلة/التنزيلات) بلا بديل جاهز.
-- ═══════════════════════════════════════════════════════════════════════
